class Solution {
  public:
    bool isBSTTraversal(vector<int>& arr) {
        
        bool flag=true;
        for(int i=0; i<arr.size()-1; i++){
            if(arr[i] < arr[i+1]) continue;
            else flag = false;
        }
        
        return flag;
    }
};
